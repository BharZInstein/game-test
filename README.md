# Sol Ridge Road

A browser-based scenic driving toy inspired by Slow Roads. The scene is live immediately; press `W` to start cruising, use `A` and `D` to steer, and `S` to slow down.

## Run locally

```bash
npm install
npm run dev
```

Open the local Vite URL in Chrome.

## Build

```bash
npm run build
```
